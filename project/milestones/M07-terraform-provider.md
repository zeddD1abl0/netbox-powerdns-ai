---
id: M07
title: Terraform/OpenTofu provider
status: planned # planned | in-progress | done
started:
closed:
---

# M07: Terraform/OpenTofu provider

## Goal

Manage nbpdns's own configuration as code with Terraform and OpenTofu (REQ-017).

## Scope (provisional)

- A separate repository, built with terraform-plugin-framework (Q-042)
- Resources for server groups, sync policies, sinks, roles, tokens and settings (Q-041)

## Design, non-goals and acceptance criteria

To be written in this milestone's plan-mode session, before implementation
starts. The milestone's branch is `m07-terraform-provider` (ADR-0010).
