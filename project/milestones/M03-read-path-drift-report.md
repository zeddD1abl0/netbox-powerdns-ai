---
id: M03
title: Read path and drift report
status: planned # planned | in-progress | done
started:
closed:
---

# M03: Read path and drift report

## Goal

The first safe value: compare NetBox with PowerDNS and report every difference, without writing anything.

## Scope (provisional)

- NetBox DNS plugin client and PowerDNS API client (ADR-0006)
- Server groups and how zones are assigned to them (ADR-0007)
- Read-only drift report in the UI, the API and metrics
- End-to-end trace context from NetBox changelog identity (Q-037)

## Design, non-goals and acceptance criteria

To be written in this milestone's plan-mode session, before implementation
starts. The milestone's branch is `m03-read-path-drift-report` (ADR-0010).
